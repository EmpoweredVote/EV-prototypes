{
  "name": "ev-prototypes",
  "version": "1.0.0",
  "description": "Feature prototypes collection",
  "scripts": {
    "build:all": "npm run build:read-rank && npm run build:data-entry",
    "build:read-rank": "cd read-rank && npm install && npm run build",
    "build:data-entry": "cd data-entry && npm install && npm run build"
  }
}
